pub fn count_triplets(a: Vec<i32>) -> i32 {
    let solutions: Vec<Vec<Vec<i32>>> = vec![];
    let mut count = 0;
    for (i, _) in a.iter().enumerate() {
        for (j, _) in a.iter().enumerate() {
            for (k, _) in a.iter().enumerate() {}

            if 
        }
    }

    count
}

#[test]
fn passes_example_one() {
    let num1 = vec![2, 1, 3];
    assert_eq!(count_triplets(a: Vec<i32>)(num1), 12)
}
